apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
spec:
  type: NodePort
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: {{ .Values.service.nodePort }}
  selector:
    app: {{ .Chart.Name }}
